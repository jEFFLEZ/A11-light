<UserControl x:Class="Qflush.A11.QflushToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="300">
    <Grid Background="#111827">
        <Border Margin="4" CornerRadius="10" Background="#020617">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Barre de titre interne QFLUSH -->
                <DockPanel Grid.Row="0" Background="#0f172a" LastChildFill="True">
                    <TextBlock Text="QFLUSH · A-11"
                               Foreground="#e5e7eb"
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               Margin="8,0" />
                </DockPanel>

                <!-- Zone qui va contenir le WebView2 -->
                <Grid Grid.Row="1" Margin="0" Name="WebViewHost" />
            </Grid>
        </Border>
    </Grid>
</UserControl>